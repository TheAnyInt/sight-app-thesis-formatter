# Authentication (set to 'false' to disable JWT verification)
AUTH_ENABLED=true

# Casdoor JWT Validation (JWKS URI) - only used when AUTH_ENABLED=true
CASDOOR_JWKS_JSON=http://localhost:8000/.well-known/jwks

# Gateway Configuration (优先使用，如果配置了则忽略 OpenAI 直连配置)
# 当配置了 GATEWAY_URL 时，用户 JWT token 需要通过请求 Authorization header 传递
GATEWAY_URL=http://your-gateway-url

# OpenAI Configuration (当 Gateway 未配置时使用)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o

# Server Configuration
PORT=3000
